"""
if we input() why do we need command line argument?
we need command line for sure and there shouldnt be a programming language without command line
Are command line really helpful?
no i will not remove them in any versions of python they are useful
if we want to remove input or  command line then what to remove?
we will remove input

command line arguments used to interact with other languages

import sys
print(sys.argv)
arg-argument
v ->vector
in C argc
arg->argument
c->count
sys.argv-->list of strings(type)



"""